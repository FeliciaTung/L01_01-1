package ui;

public class NextButton extends Button {
    /**
     * Next button that will use to go to a new page / next page for assignment/question addition
     * @param id id where to go next
     */
    NextButton(int id) {
        super("NEXT");
        this.id = id;
    }
}
